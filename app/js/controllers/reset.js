'use strict';

socialhive.controller('resetController', ['$scope', '$rootScope', '$location', '$timeout', '$interval', '$document', 'AuthenticationService',
    function($scope, $rootScope, $location, $timeout, $interval, $document, AuthenticationService) {
      $scope.progress = false;

    }
]);
