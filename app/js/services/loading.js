socialhive.factory('LoadingService', ['$rootScope', '$location',
    function ($http, $cookieStore, $rootScope, $location, XhrService) {

      var service = {};

      

        return service;
    }])
